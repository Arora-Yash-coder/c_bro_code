#include <stdio.h>

int main(){
    // This is a comment.
    /*  
        This is a
        multi-line 
        comment.
    */
    // a comment is some text that is ignored by the compiler that is used as an explanation
    // description or a note for yourself or anyone else reading over your code

    /*
        an escape sequence is a character combination consisting of a backslash followed by a
        letter or combination of digits they specify actions within a line of text.
        \n = newline
        \t = tab
    */
   printf("Hello World!\n");
   printf("\"I like Pizza\" - Yash");
   return 0;

}